{
  "name": "@features/recipe-tile",
  "version": "1.0.0",
  "main": "src/index.tsx",
  "scripts": {
    "dev": "yarn run --top-level start-storybook -p 6006",
    "test:unit": "yarn run --top-level jest",
    "lint": "yarn run --top-level eslint ./src/**.(js|jsx|ts|tsx)",
    "typecheck": "yarn run --top-level tsc --noEmit"
  },
  "peerDependencies": {
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "@library/import-types": "workspace:*",
    "@testing-library/react": "12.1.2",
    "@testing-library/react-hooks": "^3.4.2",
    "react-test-renderer": "16.13.1"
  }
}

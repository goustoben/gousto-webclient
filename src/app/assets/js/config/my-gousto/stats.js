'use strict';

const CORE = require('../core');

const STATS = {
	ROUTES: {
		API: {
			ORDER_COUNT: CORE.ROUTES.ORDER_COUNT,
		}
	},

	MULTIPLIER: {
		WASTE: 1.9,
		TIME: 66
	}
};

module.exports = STATS;

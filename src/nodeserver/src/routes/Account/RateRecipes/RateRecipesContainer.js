import { connect } from 'react-redux'
import RateRecipes from './RateRecipes'

function mapStateToProps() {
	return {

	}
}

const RateRecipesContainer = connect(mapStateToProps, {

})(RateRecipes)

export default RateRecipesContainer

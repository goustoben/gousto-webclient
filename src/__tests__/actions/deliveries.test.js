import deliveries from 'actions/deliveries'

import actionTypes from 'actions/actionTypes'

describe('delivery actions', () => {
  describe('delivery day action', () => {
    test('should dispatch ', () => {

    })
  })

  describe('delivery slot action', () => {

  })
})

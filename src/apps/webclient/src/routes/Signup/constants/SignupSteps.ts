/**
 * Steps in Wizard. If experiment steps should be added, add them there.
 */
// eslint-disable-next-line no-shadow
export enum SignupSteps {
  BOX_SIZE = 'boxSize',
  POSTCODE = 'postcode',
  DELIVERY = 'delivery',
}

import { connect } from 'react-redux'
import { MoveRecipeButton } from './MoveRecipeButton'

const MoveRecipeButtonContainer = connect(null, null)(MoveRecipeButton)

export { MoveRecipeButtonContainer }

/* eslint-disable no-unused-vars */
import { OrderedMap as ImmutableOrderedMap } from 'immutable'
import { Collection } from './collection'

export type Collections = ImmutableOrderedMap<string, Collection>

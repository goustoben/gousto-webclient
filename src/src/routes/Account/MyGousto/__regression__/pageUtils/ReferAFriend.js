export const selectReferralCount = () => cy.get('[data-testing=referralCount]')

export const selectReferralCredit = () => cy.get('[data-testing=referralCredit]')

export const selectCTA = () => cy.get('[data-testing=inviteMoreFriends]')

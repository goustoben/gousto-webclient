import React from 'react'
import css from './Subscription.css'

const Subscription = () => (
  <div className={css.subscriptionPage}>
    <h2 className={css.heading}>Subscription settings</h2>
  </div>
)

export { Subscription }

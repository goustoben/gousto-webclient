import { connect } from 'react-redux'

import GetHelp from './GetHelp'

const mapStateToProps = (state, ownProps) => (ownProps)

const GetHelpContainer = connect(mapStateToProps, {})(GetHelp)

export default GetHelpContainer
